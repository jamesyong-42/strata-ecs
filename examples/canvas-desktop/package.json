{
  "name": "@strata-ecs/example-canvas-desktop",
  "version": "0.0.0",
  "private": true,
  "description": "The canvas-editor as a minimal Electron app: same renderer code, an IPC switchboard in main (P1), and a truffle/tsnet mesh so instances collab over a tailnet with no server (P2).",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "package": "node scripts/package.mjs"
  },
  "dependencies": {
    "@vibecook/truffle": "0.6.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.2",
    "electron": "^43.1.0"
  }
}
