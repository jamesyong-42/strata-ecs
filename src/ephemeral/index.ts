/**
 * Part IV — The Ephemeral Layer (public entry: `strata/ephemeral`).
 *
 * A writable, writer-partitioned, LWW/TTL ephemeral entity store over Loro's
 * EphemeralStore (presence, previews). Built on Part II. Not implemented yet.
 */
export {};
