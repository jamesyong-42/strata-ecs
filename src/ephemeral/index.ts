/**
 * Part IV — The Ephemeral Layer (public entry: `strata-ecs/ephemeral`).
 *
 * A writable, writer-partitioned, LWW/TTL ephemeral entity store over Loro's
 * EphemeralStore (presence, previews). Built on Part II. Not implemented yet —
 * importing this entry throws so a misconfigured build fails loudly instead of
 * silently resolving to nothing.
 */
throw new Error("strata-ecs/ephemeral ships in Part IV (see docs/design.md Part IV)");
